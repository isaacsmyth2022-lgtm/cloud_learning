# cloud_learning
Highly efficient synthetic atmospheric cloud generator coupled with Blender's ray-tracing engine to enable physical shadow production. These shadows are then input as training data for a CNN.
